# Layer 5 (Document Level) Sub-Step System Design
# 第5层（文档级）子步骤系统设计

> Version: 1.1
> Date: 2026-01-07
> Purpose: Design comprehensive sub-step workflow for document-level De-AIGC processing
> Update: Added Step 1.0 Term Locking (词汇锁定)

---

## 一、所有全文级检测能力汇总
## All Document-Level Detection Capabilities Summary

### 1.1 已集成 - SmartStructureAnalyzer (LLM驱动)
### Integrated - SmartStructureAnalyzer (LLM-driven)

| 检测类型 | 英文 | 加分 | 说明 |
|---------|------|------|------|
| 线性流动 | linear_flow | +20 | First...Second...Third 枚举模式 |
| 重复模式 | repetitive_pattern | +15 | 章节结构重复 |
| 均匀长度 | uniform_length | +10 | 段落长度均匀 |
| 可预测顺序 | predictable_order | +10 | 公式化 intro-body-conclusion |
| 对称结构 | symmetry | +15 | 完美对称的章节结构 |
| 显性连接词 | explicit_connectors | - | 段首连接词检测 |
| 逻辑断点 | logic_breaks | - | 段落间过渡分析 |

### 1.2 未集成 - StructurePredictabilityAnalyzer (纯规则)
### Not Integrated - StructurePredictabilityAnalyzer (Pure Rule-based)

| 维度 | 权重 | AI特征 | 人类特征 |
|------|------|--------|---------|
| 推进可预测性 Progression | 25% | First/Second/Third单调递进 | 回溯、条件、局部反转 |
| 功能均匀性 Function | 20% | 每段落功能相似 | 非对称：深入一点，扫过其他 |
| 闭合强度 Closure | 20% | "In conclusion" 明确总结 | 开放问题、未解决张力 |
| 长度规律性 Length | 15% | 段落长度均匀(CV<0.2) | 段落长度参差(CV>0.5) |
| 连接词显性度 Connector | 20% | Furthermore/Moreover显性连接 | 词汇回声隐性连接 |
| 词汇回声 Lexical Echo | 附加 | - | 相邻段落关键词自然重复 |

### 1.3 未集成 - AnchorDensityAnalyzer (纯规则)
### Not Integrated - AnchorDensityAnalyzer (Pure Rule-based)

| 锚点类型 | 说明 | 权重 |
|---------|------|------|
| DECIMAL_NUMBER | 14.2, 3.5 | 1.0 |
| PERCENTAGE | 50%, 14.2% | 1.2 |
| STATISTICAL_VALUE | p < 0.05, r = 0.82 | 1.5 |
| CITATION | [1], (Smith, 2020) | 1.5 |
| UNIT_MEASUREMENT | 5mL, 20°C | 1.3 |
| CHEMICAL_FORMULA | H2O, CO2 | 1.2 |

**风险判定**：
- 密度 < 5.0 (每100词) = 高风险幻觉
- 密度 5.0-10.0 = 中等风险
- 密度 > 10.0 = 低风险

### 1.4 已集成 - 段落长度分布分析
### Integrated - Paragraph Length Distribution Analysis

| 指标 | 阈值 | 说明 |
|------|------|------|
| CV (变异系数) | < 0.30 | 过于均匀 = AI特征 |
| 目标 CV | ≥ 0.40 | 人类学术写作的目标 |

**策略类型**：merge(合并), expand(扩展), split(拆分), compress(压缩)

### 1.5 已集成 - TransitionAnalyzer (段落衔接)
### Integrated - TransitionAnalyzer (Paragraph Transition)

| 问题类型 | 说明 |
|---------|------|
| explicit_connector | 显性连接词过多 |
| too_smooth | 过度平滑过渡 |
| abrupt | 突兀过渡 |
| repetitive_opener | 重复开头 |
| ai_perfect_linear | AI式完美线性过渡 |
| topic_sentence_pattern | 公式化主题句 |
| summary_ending | 公式化总结结尾 |

---

## 二、优先级、兼容性、依赖性、冲突性分析
## Priority, Compatibility, Dependency, Conflict Analysis

### 2.1 优先级分析 | Priority Analysis

#### 高优先级（结构性问题，影响全局）| High Priority (Structural)

| 问题 | 优先级 | 理由 |
|------|--------|------|
| B 公式化章节顺序 | ★★★★★ | 章节顺序是全文骨架，必须最先确定 |
| A+D 章节对称结构/均匀段落数 | ★★★★☆ | 章节层面的结构问题，影响后续所有分析 |

#### 中优先级（章节内容模式）| Medium Priority (Content Pattern)

| 问题 | 优先级 | 理由 |
|------|--------|------|
| C 章节均匀长度 | ★★★☆☆ | 章节长度问题，依赖章节结构稳定 |
| F+G 逻辑重复/线性流动 | ★★★☆☆ | 章节内的逻辑模式，需在结构稳定后检测 |

#### 低优先级（细节问题，依赖前面稳定）| Low Priority (Detail)

| 问题 | 优先级 | 理由 |
|------|--------|------|
| E 段落均匀长度 | ★★☆☆☆ | 段落层面问题，最后处理 |
| H+I+J 连接词/语义回声/断裂点 | ★★☆☆☆ | 段落间过渡问题，最后处理 |

---

### 2.2 兼容性分析 | Compatibility Analysis

#### 可合并的问题组 | Compatible Groups

| 组 | 包含问题 | 兼容原因 |
|----|----------|----------|
| **组1** | A (对称结构) + D (均匀段落数) + C (章节长度) | 都是章节均匀性问题，同一维度 |
| **组2** | F (重复模式) + G (线性流动) | 都是章节内逻辑模式问题 |
| **组3** | H (连接词) + I (语义回声) + J (断裂点) | **必须合并**：都是段落间过渡问题，有冲突需综合处理 |

---

### 2.3 依赖性分析 | Dependency Analysis

```
Step 1.0 (术语锁定)
    ↓
B (章节顺序识别) ← 最基础，所有分析依赖正确的章节识别
    ↓
A+D+C (章节均匀性) ← 依赖章节边界确定
    ↓
F+G (逻辑模式) ← 依赖章节结构稳定
    ↓
E (段落长度) ← 依赖前面所有结构稳定
    ↓
H+I+J (过渡问题) ← 依赖段落内容稳定
```

---

### 2.4 冲突性分析 | Conflict Analysis

#### 冲突矩阵 | Conflict Matrix

| 先A后B | 冲突描述 | 解决方案 |
|--------|----------|----------|
| H→J | 先删连接词，可能产生新的逻辑断裂 | **合并处理**，同时检测后综合决策 |
| J→H | 先补逻辑断裂，可能添加连接词 | **合并处理**，用语义回声而非连接词 |
| E→C | 先调段落长度，章节长度会变 | **先C后E**，章节层面优先 |
| F→E | 先处理线性流动，可能需重组段落 | **先F后E**，逻辑模式优先 |
| A→F | 先调对称性（合并/拆分章节），逻辑模式检测对象变了 | **先A后F**，结构优先于内容 |

#### 关键冲突决策 | Key Conflict Decisions

**冲突1: 显性连接词(H) vs 逻辑断裂点(J)**
```
场景：段落间使用 "Furthermore" 连接
- 如果先删 "Furthermore" → 可能产生逻辑断裂
- 如果先检测断裂点 → 可能漏检被连接词掩盖的问题

解决：合并到一个 substep，同时检测，用语义回声替代连接词
```

**冲突2: 段落长度(E) vs 逻辑模式(F)**
```
场景：检测到 First-Second-Third 线性模式
- 如果先调段落长度（拆分/合并）→ 线性模式可能被打乱或强化
- 如果先处理线性模式 → 段落可能需要重组

解决：先F后E，逻辑模式是更本质的AI特征
```

---

### 2.5 设计原则总结 | Design Principles Summary

```
原则1：从宏观到微观
  - 先检测全局结构问题（章节顺序、对称性）
  - 再检测章节级问题（逻辑模式、长度）
  - 最后检测段落衔接问题（连接词、断裂点）

原则2：结构优先于内容
  - 先确定骨架（章节结构）
  - 再填充内容（逻辑、长度）
  - 最后润色过渡

原则3：冲突问题同步处理
  - H+I+J 必须合并，避免互相打乱
  - 同时检测，综合决策

原则4：处理后传递
  - 每个子步骤完成后，将修改后的文本传递给下一步
  - 确保后续检测基于最新文本
```

### 2.2 用户交互模式
### User Interaction Pattern

```
每个子步骤的交互流程：
1. 检测 → 显示问题列表
2. 用户点击问题 → 触发AI分析
3. AI提供改进建议 + 改写提示词
4. 用户选择：
   - 接受AI建议自动修改
   - 手动修改
   - 跳过
5. 完成后 → 传递给下一子步骤
```

---

## 三、Layer 5 子步骤设计方案（基于冲突分析优化）
## Layer 5 Sub-Step Design Proposal (Optimized based on Conflict Analysis)

### 执行流程图
### Execution Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    Layer 5: Document Level Analysis                          │
│                    文档级分析（基于优先级和依赖性排序）                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐     │
│  │  Step 1.0: 术语锁定 (Term Locking) ⭐                               │     │
│  │  ├── LLM提取专业名词和高频实义词                                    │     │
│  │  ├── 用户多选确认锁定词汇                                          │     │
│  │  └── 锁定词汇传递到后续所有LLM步骤                                  │     │
│  │                                                                     │     │
│  │  检测器：LLM Term Extractor                                        │     │
│  │  输出：locked_terms[] → 传递到所有后续步骤                          │     │
│  └────────────────────────────────────────────────────────────────────┘     │
│                                    ↓                                         │
│  ┌────────────────────────────────────────────────────────────────────┐     │
│  │  Step 1.1: 章节结构与顺序识别 (Section Structure & Order)          │     │
│  │  ├── 识别章节结构 (Section Boundary Detection)                     │     │
│  │  └── 检测公式化章节顺序 (Predictable Order: Intro-Method-Results)  │     │
│  │                                                                     │     │
│  │  检测项：B (公式化章节顺序)                                         │     │
│  │  优先级：★★★★★ (最高 - 所有后续分析依赖正确的章节识别)              │     │
│  │  检测器：SmartStructureAnalyzer (LLM-based)                        │     │
│  │  输出：sections[] + orderIssue                                      │     │
│  └────────────────────────────────────────────────────────────────────┘     │
│                                    ↓                                         │
│  ┌────────────────────────────────────────────────────────────────────┐     │
│  │  Step 1.2: 章节均匀性检测 (Section Uniformity Detection) 【合并组1】│     │
│  │  ├── 章节对称结构 (Section Symmetry) - A                           │     │
│  │  ├── 章节均匀段落数量 (Uniform Paragraph Count per Section) - D    │     │
│  │  └── 章节均匀长度 (Uniform Section Length) - C                     │     │
│  │                                                                     │     │
│  │  检测项：A + D + C (同为章节均匀性问题，兼容性高)                    │     │
│  │  优先级：★★★★☆ (依赖1.1的章节边界)                                 │     │
│  │  检测器：统计计算 + LLM分析                                         │     │
│  │  输出：symmetryIssues[] + lengthIssues[] + suggestions[]            │     │
│  └────────────────────────────────────────────────────────────────────┘     │
│                                    ↓                                         │
│  ┌────────────────────────────────────────────────────────────────────┐     │
│  │  Step 1.3: 章节逻辑模式检测 (Section Logic Pattern) 【合并组2】     │     │
│  │  ├── 章节逻辑重复模式 (Repetitive Pattern: 问题-分析-结论) - F     │     │
│  │  └── 章节逻辑线性流动 (Linear Flow: First-Second-Third) - G        │     │
│  │                                                                     │     │
│  │  检测项：F + G (同为章节内逻辑模式问题)                              │     │
│  │  优先级：★★★☆☆ (依赖1.2的章节结构稳定)                             │     │
│  │  冲突说明：必须在段落长度调整前处理，否则会互相打乱                   │     │
│  │  检测器：StructurePredictabilityAnalyzer + LLM分析                  │     │
│  │  输出：repetitivePatterns[] + linearFlowIssues[] + suggestions[]    │     │
│  └────────────────────────────────────────────────────────────────────┘     │
│                                    ↓                                         │
│  ┌────────────────────────────────────────────────────────────────────┐     │
│  │  Step 1.4: 段落长度均匀性检测 (Paragraph Length Uniformity)        │     │
│  │  └── 全文段落长度是否过于均匀 (CV < 0.30 = AI特征) - E             │     │
│  │                                                                     │     │
│  │  检测项：E (段落均匀长度)                                           │     │
│  │  优先级：★★☆☆☆ (依赖前面所有结构稳定)                              │     │
│  │  冲突说明：必须在逻辑模式处理后进行，避免破坏1.3的结果               │     │
│  │  检测器：ParagraphLengthAnalysis (统计) + LLM建议                   │     │
│  │  输出：lengthCV + uniformParagraphs[] + merge/split策略              │     │
│  └────────────────────────────────────────────────────────────────────┘     │
│                                    ↓                                         │
│  ┌────────────────────────────────────────────────────────────────────┐     │
│  │  Step 1.5: 段落过渡检测 (Paragraph Transition) 【合并组3-必须合并】 │     │
│  │  ├── 显性连接词过度使用 (Explicit Connector Overuse) - H           │     │
│  │  ├── 缺乏语义回声 (Missing Semantic Echo) - I                      │     │
│  │  └── 逻辑断裂点 (Logic Break Points) - J                           │     │
│  │                                                                     │     │
│  │  检测项：H + I + J (三者有冲突，必须同时检测、综合处理)              │     │
│  │  优先级：★★☆☆☆ (最后处理，依赖段落内容稳定)                        │     │
│  │  冲突说明：                                                         │     │
│  │    - H→J冲突：先删连接词可能产生逻辑断裂                            │     │
│  │    - J→H冲突：先补断裂可能添加连接词                                │     │
│  │    - 解决：同时检测，用语义回声(I)统一替换                          │     │
│  │  检测器：TransitionAnalyzer + StructurePredictabilityAnalyzer + LLM │     │
│  │  输出：explicitConnectors[] + logicBreaks[] + semanticEchoReplacements[]│   │
│  └────────────────────────────────────────────────────────────────────┘     │
│                                    ↓                                         │
│                     传递修改后的文本到 Layer 4 (Section)                     │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 冲突解决流程图 | Conflict Resolution Flow

```
                    Step 1.5 内部冲突解决机制
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐                   │
│   │ 检测连接词(H) │   │ 检测断裂点(J) │   │ 检测语义回声(I)│                   │
│   └──────┬───────┘   └──────┬───────┘   └──────┬───────┘                   │
│          │                  │                  │                            │
│          └────────────┬─────┴──────────────────┘                            │
│                       ↓                                                      │
│          ┌───────────────────────────┐                                      │
│          │    综合分析（同时考虑）    │                                      │
│          │                           │                                      │
│          │ 对于每个段落过渡点：       │                                      │
│          │ 1. 是否有显性连接词？      │                                      │
│          │ 2. 是否有逻辑断裂？        │                                      │
│          │ 3. 前段关键概念是什么？    │                                      │
│          └───────────┬───────────────┘                                      │
│                      ↓                                                       │
│          ┌───────────────────────────┐                                      │
│          │    统一解决方案生成        │                                      │
│          │                           │                                      │
│          │ 如果有连接词：            │                                      │
│          │   → 删除连接词            │                                      │
│          │   → 生成语义回声替换      │                                      │
│          │                           │                                      │
│          │ 如果有断裂但无连接词：    │                                      │
│          │   → 生成语义回声桥接      │                                      │
│          │   → 不使用显性连接词      │                                      │
│          └───────────────────────────┘                                      │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 四、各子步骤详细设计
## Detailed Design for Each Sub-Step

### Step 1.0: 词汇锁定 (Term Locking) ⭐ NEW

**目的**：在所有检测和改写步骤之前，锁定专业术语和核心词汇，确保这些词汇在后续所有LLM处理步骤中保持不变。

**Purpose**: Before all detection and rewriting steps, lock technical terms and core vocabulary to ensure these terms remain unchanged in all subsequent LLM processing steps.

**核心功能**：
| 功能 | 说明 |
|------|------|
| LLM术语提取 | 调用LLM分析全文，提取专业名词、高频实义词汇、关键词组 |
| 用户多选确认 | 展示提取结果，用户选择需要锁定的词汇 |
| 锁定规则传递 | 将锁定词汇列表传递到本次任务的所有后续LLM步骤 |

**提取类型**：
| 类型 Type | 说明 Description | 示例 Examples |
|-----------|-----------------|---------------|
| 专业术语 Technical Terms | 学科领域特有词汇 | "machine learning", "neural network", "土壤盐渍化" |
| 专有名词 Proper Nouns | 人名、地名、机构名、方法名 | "Smith et al.", "North China Plain", "AlphaFold" |
| 缩写词 Acronyms | 技术缩写 | "CNN", "LSTM", "PCR", "ANOVA" |
| 高频核心词 High-freq Core Words | 文中反复出现的关键概念 | 文章主题相关的核心词汇 |
| 关键词组 Key Phrases | 固定搭配或专业短语 | "soil salinization", "deep learning model" |

**LLM提取Prompt设计**：
```
You are an academic term extractor. Analyze this document and extract:

1. TECHNICAL TERMS: Domain-specific vocabulary that should not be changed
2. PROPER NOUNS: Names of people, places, organizations, methods
3. ACRONYMS: Technical abbreviations used in the text
4. KEY PHRASES: Important fixed expressions or collocations
5. HIGH-FREQUENCY CORE WORDS: Words appearing 3+ times that carry core meaning

For each extracted term, provide:
- term: The exact term/phrase as it appears
- type: One of [technical_term, proper_noun, acronym, key_phrase, core_word]
- frequency: How many times it appears in the document
- reason: Why this should be locked (1 sentence)
- reason_zh: Chinese explanation

Output JSON format:
{
  "extracted_terms": [
    {
      "term": "machine learning",
      "type": "technical_term",
      "frequency": 12,
      "reason": "Core technical concept of the paper",
      "reason_zh": "论文核心技术概念"
    }
  ]
}
```

**用户界面设计**：
```
┌─────────────────────────────────────────────────────────────────┐
│ Step 1.0: 词汇锁定 Term Locking                                 │
├─────────────────────────────────────────────────────────────────┤
│ 以下词汇建议锁定，锁定后在后续所有改写步骤中保持不变：           │
│ The following terms are recommended for locking:                │
│                                                                 │
│ 专业术语 Technical Terms:                                       │
│ ☑️ machine learning (12次)    ☑️ neural network (8次)          │
│ ☑️ deep learning (6次)        ☑️ convolutional layer (4次)     │
│ ☐ training data (3次)         ☐ validation set (2次)           │
│                                                                 │
│ 专有名词 Proper Nouns:                                          │
│ ☑️ AlphaFold (5次)            ☑️ ImageNet (3次)                │
│ ☑️ Smith et al. (4次)         ☑️ North China Plain (2次)       │
│                                                                 │
│ 缩写词 Acronyms:                                                │
│ ☑️ CNN (15次)                 ☑️ LSTM (8次)                    │
│ ☑️ ReLU (6次)                 ☑️ GPU (4次)                     │
│                                                                 │
│ 关键词组 Key Phrases:                                           │
│ ☑️ soil salinization (9次)    ☐ experimental results (3次)     │
│ ☑️ feature extraction (5次)   ☐ proposed method (4次)          │
│                                                                 │
│ ───────────────────────────────────────────────────────────────│
│ 手动添加 Add Custom Term: [________________] [+ 添加]           │
│                                                                 │
│ 已选择锁定: 15个词汇/词组                                       │
│ Selected for locking: 15 terms/phrases                         │
│                                                                 │
│ [全选推荐] [清除选择] [确认锁定并继续 →]                        │
└─────────────────────────────────────────────────────────────────┘
```

**锁定词汇的后续应用**：

锁定的词汇将通过以下机制应用到所有后续LLM步骤：

```python
# 锁定词汇数据结构
class LockedTerms:
    terms: List[str]           # 锁定的词汇列表
    term_patterns: List[str]   # 正则模式（用于匹配变体）
    case_sensitive: bool       # 是否区分大小写

# 在所有LLM Prompt中添加锁定词汇指令
LOCKED_TERMS_INSTRUCTION = """
## PROTECTED TERMS (DO NOT MODIFY)
The following terms must remain EXACTLY as written. Do not:
- Change the wording
- Replace with synonyms
- Translate
- Modify capitalization

Protected terms:
{locked_terms_list}

When rewriting, preserve these terms verbatim.
"""
```

**技术实现要点**：

1. **Session级存储**：锁定词汇存储在Session中，整个任务流程共享
2. **Prompt注入**：每次LLM调用时，自动将锁定词汇注入到Prompt中
3. **后处理验证**：LLM输出后检查锁定词汇是否被意外修改
4. **变体匹配**：支持词形变化匹配（如 "learn" → "learning", "learned"）

**API端点设计**：
```python
# POST /api/v1/layer5/step1-0/extract-terms
# 调用LLM提取建议锁定的词汇
class ExtractTermsRequest(BaseModel):
    document_text: str
    session_id: str

class ExtractTermsResponse(BaseModel):
    extracted_terms: List[ExtractedTerm]
    total_count: int

# POST /api/v1/layer5/step1-0/confirm-lock
# 用户确认锁定词汇
class ConfirmLockRequest(BaseModel):
    session_id: str
    locked_terms: List[str]
    custom_terms: List[str]  # 用户手动添加的词汇

class ConfirmLockResponse(BaseModel):
    locked_count: int
    locked_terms: List[str]
    message: str
```

**输出数据结构**：
```python
TermLockingResult:
  - locked_terms: List[str]           # 锁定的词汇列表
  - term_metadata: Dict[str, TermInfo] # 词汇元信息
  - total_locked: int                  # 锁定总数
  - custom_added: int                  # 用户手动添加数量
```

---

### Step 1.1: 结构框架检测 (Structure Framework Detection)

**目的**：检测全文的宏观结构是否过于公式化、对称、可预测

**检测项**：
| 检测项 | 来源 | 触发阈值 | 风险等级 |
|--------|------|----------|----------|
| 章节对称性 symmetry | SmartStructureAnalyzer | 检测到对称结构 | +15分 |
| 可预测顺序 predictable_order | SmartStructureAnalyzer | 公式化结构 | +10分 |
| 线性流动 linear_flow | SmartStructureAnalyzer | First/Second/Third | +20分 |
| 推进可预测性 | StructurePredictabilityAnalyzer | 分数≥70 | High |

**用户界面设计**：
```
┌─────────────────────────────────────────────────────────┐
│ Step 1.1: 结构框架检测 Structure Framework Detection    │
├─────────────────────────────────────────────────────────┤
│ 检测结果 Detection Results:                             │
│                                                         │
│ ⚠️ 章节对称性 Section Symmetry               [高风险]   │
│    检测到4个章节各包含3个段落，结构过于对称              │
│    [点击查看详情] [AI分析建议]                          │
│                                                         │
│ ⚠️ 线性流动模式 Linear Flow                  [高风险]   │
│    发现"First...Second...Third"枚举模式                 │
│    影响位置：2(1), 2(2), 2(3)                           │
│    [点击查看详情] [AI分析建议]                          │
│                                                         │
│ ⚠️ 可预测顺序 Predictable Order              [中风险]   │
│    文档遵循公式化的intro-body-conclusion结构            │
│    [点击查看详情] [AI分析建议]                          │
│                                                         │
├─────────────────────────────────────────────────────────┤
│ 结构分数 Structure Score: 65/100 (高风险)               │
│                                                         │
│ [合并处理所有问题] [跳过此步骤] [下一步 →]              │
└─────────────────────────────────────────────────────────┘
```

**AI分析触发后的响应**：
```
用户点击 "AI分析建议" 后：

┌─────────────────────────────────────────────────────────┐
│ 🤖 AI分析：章节对称性问题                               │
├─────────────────────────────────────────────────────────┤
│ 问题诊断：                                              │
│ 文档4个主要章节各包含3个段落，呈现完美对称结构，        │
│ 这是AI生成文本的典型特征。人类写作通常会根据内容        │
│ 重要性自然分配篇幅。                                    │
│                                                         │
│ 改进建议：                                              │
│ 1. 将第2章扩展为4-5个段落（深入论述核心论点）          │
│ 2. 压缩第3章为2个段落（次要内容简要带过）               │
│ 3. 考虑合并相关段落或拆分复杂段落                       │
│                                                         │
│ 改写提示词：                                            │
│ "请将第2章'方法论'部分扩展，增加具体实验设计细节        │
│ 和数据收集过程的描述，使其包含4-5个段落。同时将         │
│ 第3章'结果分析'中相似的内容进行合并压缩。"             │
│                                                         │
│ [应用此建议] [手动修改] [忽略]                          │
└─────────────────────────────────────────────────────────┘
```

---

### Step 1.2: 段落长度规律性检测 (Paragraph Length Regularity)

**目的**：检测段落长度是否过于均匀，以及段落功能分布是否对称

**检测项**：
| 检测项 | 来源 | 触发阈值 | 风险等级 |
|--------|------|----------|----------|
| 段落长度均匀性 | ParagraphLengthAnalysis | CV < 0.30 | Medium |
| 长度规律性 length_regularity | StructurePredictabilityAnalyzer | 分数≥70 | Medium |
| 功能均匀性 function_uniformity | StructurePredictabilityAnalyzer | 分数≥70 | Medium |
| 段落长度 uniform_length | SmartStructureAnalyzer | 检测到均匀 | +10分 |

**用户界面设计**：
```
┌─────────────────────────────────────────────────────────┐
│ Step 1.2: 段落长度规律性 Paragraph Length Regularity    │
├─────────────────────────────────────────────────────────┤
│ 段落长度分布 Paragraph Length Distribution:             │
│                                                         │
│ 平均长度: 125 词  |  CV: 0.18 (过于均匀)               │
│ 最短: 108 词  |  最长: 142 词                          │
│                                                         │
│ 段落长度可视化:                                         │
│ Para 1: ████████████░░░░░░░░ 118词 [可扩展]           │
│ Para 2: █████████████░░░░░░░ 125词                    │
│ Para 3: ██████████████░░░░░░ 135词                    │
│ Para 4: ███████████░░░░░░░░░ 108词 [建议合并]         │
│ Para 5: █████████████░░░░░░░ 128词                    │
│ Para 6: ██████████████░░░░░░ 142词 [可拆分]           │
│                                                         │
│ 建议策略 Suggested Strategies:                         │
│ 🔗 合并: Para 4 + Para 5 → 创造长段落                  │
│ ✂️ 拆分: Para 6 → 增加变化性                          │
│ 📈 扩展: Para 1 → 添加细节                            │
│                                                         │
│ [应用所有策略] [选择性应用] [下一步 →]                 │
└─────────────────────────────────────────────────────────┘
```

---

### Step 1.3: 推进模式与闭合检测 (Progression & Closure Detection)

**目的**：检测文档的逻辑推进是否过于单调，结尾是否过于公式化

**检测项**：
| 检测项 | 来源 | 触发阈值 | 风险等级 |
|--------|------|----------|----------|
| 推进可预测性 progression | StructurePredictabilityAnalyzer | 分数≥70 | High |
| 重复模式 repetitive_pattern | SmartStructureAnalyzer | 检测到重复 | +15分 |
| 闭合强度 closure | StructurePredictabilityAnalyzer | 分数≥80 | Low |

**单调推进标记词（AI模式）**：
- 顺序类：first, second, third, firstly, secondly, finally
- 累加类：furthermore, moreover, additionally, in addition
- 因果类：therefore, thus, hence, consequently, as a result

**非单调标记词（人类模式）**：
- 回溯类：as noted earlier, returning to, recall that
- 条件类：if, when, unless, provided that
- 对比类：however, but, yet, nevertheless, on the other hand
- 让步类：although, though, despite, even though

**用户界面设计**：
```
┌─────────────────────────────────────────────────────────┐
│ Step 1.3: 推进模式与闭合 Progression & Closure          │
├─────────────────────────────────────────────────────────┤
│ 推进模式分析 Progression Analysis:                      │
│                                                         │
│ 当前模式: 单调推进 (monotonic) - 高风险                │
│ 推进可预测性分数: 78/100                               │
│                                                         │
│ 检测到的单调标记词:                                     │
│ • "First" at 2(1) - 顺序类                             │
│ • "Second" at 2(2) - 顺序类                            │
│ • "Third" at 2(3) - 顺序类                             │
│ • "Furthermore" at 3(1) - 累加类                       │
│ • "Therefore" at 4(1) - 因果类                         │
│                                                         │
│ 缺少的非单调元素:                                       │
│ ❌ 回溯引用 (as noted earlier, returning to)           │
│ ❌ 条件触发 (if, when, unless)                         │
│ ❌ 对比转折 (however, but, on the other hand)          │
│                                                         │
│ [AI生成非单调化建议] [手动调整]                        │
├─────────────────────────────────────────────────────────┤
│ 闭合强度分析 Closure Analysis:                          │
│                                                         │
│ 当前类型: 强闭合 (strong) - 中风险                     │
│ 闭合强度分数: 85/100                                   │
│                                                         │
│ 检测到的强闭合模式:                                     │
│ • "In conclusion" at final paragraph                   │
│ • "This study has demonstrated..." pattern             │
│                                                         │
│ 建议: 改为开放式结尾或保留未解决问题                    │
│                                                         │
│ [AI生成开放式结尾] [保持原样] [下一步 →]               │
└─────────────────────────────────────────────────────────┘
```

---

### Step 1.4: 连接词与衔接检测 (Connectors & Transitions)

**目的**：检测显性连接词使用是否过多，段落衔接是否过于机械

**检测项**：
| 检测项 | 来源 | 触发阈值 | 风险等级 |
|--------|------|----------|----------|
| 显性连接词 explicit_connector | TransitionAnalyzer | 段首检测到 | High |
| 连接词显性度 connector_explicitness | StructurePredictabilityAnalyzer | 分数≥70 | High |
| 过度平滑 too_smooth | TransitionAnalyzer | 检测到 | High |
| AI完美线性 ai_perfect_linear | TransitionAnalyzer | 检测到 | High |
| 词汇回声缺失 | StructurePredictabilityAnalyzer | 分数<0.3 | Medium |

**用户界面设计**：
```
┌─────────────────────────────────────────────────────────┐
│ Step 1.4: 连接词与衔接 Connectors & Transitions         │
├─────────────────────────────────────────────────────────┤
│ 显性连接词检测 Explicit Connector Detection:            │
│                                                         │
│ 连接词显性度分数: 82/100 (高风险)                      │
│ 连接词密度: 45% 段落以显性连接词开头                   │
│                                                         │
│ 检测到的问题连接词:                                     │
│ ┌─────────────────────────────────────────────────────┐│
│ │ 位置: 1(2) | 连接词: "Furthermore"                  ││
│ │ 原文: "Furthermore, the results demonstrate..."     ││
│ │ 前段结尾: "...statistical significance p<0.05."    ││
│ │ 关键概念: [statistical significance, p-value]       ││
│ │                                                     ││
│ │ 语义回声替换建议:                                   ││
│ │ "This pattern of statistical significance extends   ││
│ │ to the secondary analysis, where..."                ││
│ │                                                     ││
│ │ [应用替换] [修改后应用] [跳过]                      ││
│ └─────────────────────────────────────────────────────┘│
│                                                         │
│ ┌─────────────────────────────────────────────────────┐│
│ │ 位置: 2(1) | 连接词: "Moreover"                     ││
│ │ ...                                                 ││
│ └─────────────────────────────────────────────────────┘│
│                                                         │
│ 词汇回声分析 Lexical Echo Analysis:                     │
│ 当前词汇回声分数: 0.25 (偏低)                          │
│ 建议: 在段落过渡处自然引用上段关键概念                  │
│                                                         │
│ [批量替换所有连接词] [逐个处理] [下一步 →]             │
└─────────────────────────────────────────────────────────┘
```

---

### Step 1.5: 内容实质性检测 (Content Substantiveness)

**目的**：检测长段落是否缺乏实质性内容（学术锚点），识别可能的AI填充物

**检测项**：
| 检测项 | 来源 | 触发阈值 | 风险等级 |
|--------|------|----------|----------|
| 锚点密度 anchor_density | AnchorDensityAnalyzer | < 5.0/100词 | High |
| 幻觉风险 hallucination_risk | AnchorDensityAnalyzer | 高风险标记 | High |

**用户界面设计**：
```
┌─────────────────────────────────────────────────────────┐
│ Step 1.5: 内容实质性 Content Substantiveness            │
├─────────────────────────────────────────────────────────┤
│ 学术锚点密度分析 Academic Anchor Density:               │
│                                                         │
│ 整体锚点密度: 7.2 / 100词 (中等)                       │
│ 文档级幻觉风险: 中等                                   │
│                                                         │
│ 高风险段落 High-Risk Paragraphs:                       │
│ ┌─────────────────────────────────────────────────────┐│
│ │ 段落 3(2) | 词数: 156 | 锚点密度: 2.5 [高风险]     ││
│ │                                                     ││
│ │ 预览: "The comprehensive landscape of modern       ││
│ │ agricultural practices underscores the             ││
│ │ multifaceted nature of sustainable farming..."     ││
│ │                                                     ││
│ │ 检测到的锚点: 0个数据, 0个引用, 0个单位             ││
│ │                                                     ││
│ │ ⚠️ 警告: 此段落可能是AI填充物，缺乏具体证据        ││
│ │                                                     ││
│ │ 建议:                                              ││
│ │ • 添加具体数据（如: "产量提升14.2%"）              ││
│ │ • 添加引用支撑（如: "(Smith et al., 2023)"）       ││
│ │ • 添加具体案例或实验结果                           ││
│ │                                                     ││
│ │ [AI生成改进建议] [标记需要补充] [保持原样]         ││
│ └─────────────────────────────────────────────────────┘│
│                                                         │
│ 锚点类型分布 Anchor Type Distribution:                 │
│ • 小数/数值: 12个 (25%)                               │
│ • 百分比: 8个 (17%)                                   │
│ • 引用: 15个 (31%)                                    │
│ • 单位: 6个 (13%)                                     │
│ • 其他: 7个 (14%)                                     │
│                                                         │
│ [处理所有高风险段落] [完成Step 1] [进入Layer 4 →]      │
└─────────────────────────────────────────────────────────┘
```

---

## 五、技术实现要点
## Technical Implementation Notes

### 5.1 API端点设计
### API Endpoint Design

```python
# Layer 5 Sub-step Endpoints

# Step 1.0: Term Locking (NEW)
POST /api/v1/layer5/step1-0/extract-terms  # LLM提取建议锁定词汇
POST /api/v1/layer5/step1-0/confirm-lock   # 用户确认锁定词汇
GET  /api/v1/layer5/step1-0/locked-terms   # 获取当前锁定词汇列表

POST /api/v1/layer5/step1-1/analyze  # 结构框架检测
POST /api/v1/layer5/step1-1/process  # 处理结构问题

POST /api/v1/layer5/step1-2/analyze  # 段落长度检测
POST /api/v1/layer5/step1-2/process  # 应用长度策略

POST /api/v1/layer5/step1-3/analyze  # 推进模式检测
POST /api/v1/layer5/step1-3/process  # 非单调化处理

POST /api/v1/layer5/step1-4/analyze  # 连接词检测
POST /api/v1/layer5/step1-4/process  # 语义回声替换

POST /api/v1/layer5/step1-5/analyze  # 锚点密度检测
POST /api/v1/layer5/step1-5/process  # 标记/补充建议
```

### 5.2 检测器集成
### Detector Integration

```python
# Step 1.0: Term Locking (NEW)
# LLM-based term extraction, no traditional detectors
processor = LLMTermExtractor  # Claude/GPT for term extraction

# Step 1.1: Structure Framework
detectors = [
    SmartStructureAnalyzer,  # LLM-based: symmetry, predictable_order, linear_flow
    StructurePredictabilityAnalyzer,  # Rule-based: progression predictability
]

# Step 1.2: Paragraph Length
detectors = [
    ParagraphLengthAnalysis,  # CV analysis, strategies
    StructurePredictabilityAnalyzer,  # length_regularity, function_uniformity
]

# Step 1.3: Progression & Closure
detectors = [
    StructurePredictabilityAnalyzer,  # progression, closure
    SmartStructureAnalyzer,  # repetitive_pattern
]

# Step 1.4: Connectors & Transitions
detectors = [
    TransitionAnalyzer,  # explicit_connectors, too_smooth, ai_perfect_linear
    StructurePredictabilityAnalyzer,  # connector_explicitness, lexical_echo
]

# Step 1.5: Content Substantiveness
detectors = [
    AnchorDensityAnalyzer,  # anchor_density, hallucination_risk
]
```

### 5.3 数据流设计
### Data Flow Design

```
┌──────────────────────────────────────────────────────────────────┐
│                         数据流 Data Flow                          │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  原始文本 Original Text                                          │
│       ↓                                                          │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │ Step 1.0: 词汇锁定 Term Locking                            │  │
│  │   ├── LLM提取建议词汇                                      │  │
│  │   ├── 用户多选确认                                         │  │
│  │   └── 输出: locked_terms[] → 存入Session                   │  │
│  └────────────────────────────────────────────────────────────┘  │
│       ↓                                                          │
│  ════════════════════════════════════════════════════════════    │
│  ║  locked_terms 传递到所有后续步骤的LLM Prompt中  ║              │
│  ════════════════════════════════════════════════════════════    │
│       ↓                                                          │
│  Step 1.1 (+ locked_terms) → 修改后文本A → Session存储          │
│       ↓                                                          │
│  Step 1.2 (+ locked_terms, 基于文本A) → 修改后文本B → Session   │
│       ↓                                                          │
│  Step 1.3 (+ locked_terms, 基于文本B) → 修改后文本C → Session   │
│       ↓                                                          │
│  Step 1.4 (+ locked_terms, 基于文本C) → 修改后文本D → Session   │
│       ↓                                                          │
│  Step 1.5 (+ locked_terms, 基于文本D) → 最终文本 → 传递给L4     │
│       ↓                                                          │
│  ════════════════════════════════════════════════════════════    │
│  ║  locked_terms 继续传递到 Layer 4, 3, 2, 1 的所有LLM步骤  ║    │
│  ════════════════════════════════════════════════════════════    │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘

锁定词汇传递机制 Locked Terms Propagation:
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  Session Context:                                                │
│  {                                                               │
│    "session_id": "xxx",                                          │
│    "locked_terms": ["machine learning", "CNN", "AlphaFold"...], │
│    "current_text": "...",                                        │
│    "step_history": [...]                                         │
│  }                                                               │
│                                                                  │
│  每次LLM调用时自动注入:                                          │
│  prompt = base_prompt + LOCKED_TERMS_INSTRUCTION.format(         │
│      locked_terms_list=session.locked_terms                      │
│  )                                                               │
│                                                                  │
│  LLM输出后验证:                                                  │
│  for term in session.locked_terms:                               │
│      if term in original and term not in output:                 │
│          raise TermModificationError(term)                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 六、优先级与实现计划
## Priority and Implementation Plan

| 优先级 | 子步骤 | 需要集成的检测器 | 复杂度 |
|--------|--------|-----------------|--------|
| **P0** | **Step 1.0 词汇锁定** | **LLM Term Extractor (新建)** | **中** |
| P1 | Step 1.4 连接词与衔接 | TransitionAnalyzer (已有) + StructurePredictabilityAnalyzer | 中 |
| P1 | Step 1.2 段落长度 | ParagraphLengthAnalysis (已有) + StructurePredictabilityAnalyzer | 低 |
| P2 | Step 1.3 推进模式与闭合 | StructurePredictabilityAnalyzer (需完整集成) | 中 |
| P2 | Step 1.1 结构框架 | SmartStructureAnalyzer (已有) + StructurePredictabilityAnalyzer | 中 |
| P3 | Step 1.5 内容实质性 | AnchorDensityAnalyzer (需集成) | 中 |

> **注意**: Step 1.0 词汇锁定是最高优先级(P0)，因为它必须在所有其他步骤之前完成，以确保锁定词汇能够传递到后续所有LLM调用中。
> **Note**: Step 1.0 Term Locking is highest priority (P0) because it must complete before all other steps to ensure locked terms are passed to all subsequent LLM calls.

---

## 七、总结
## Summary

本设计将Layer 5的文档级检测整合为**6个有序的子步骤**：

0. **Step 1.0 词汇锁定** ⭐ - LLM提取专业术语和高频实义词，用户多选确认锁定，锁定词汇传递到后续所有LLM步骤
1. **Step 1.1 结构框架检测** - 检测全局结构的对称性、可预测性、线性流动
2. **Step 1.2 段落长度规律性** - 检测段落长度均匀性和功能分布
3. **Step 1.3 推进模式与闭合** - 检测单调推进和公式化结尾
4. **Step 1.4 连接词与衔接** - 检测显性连接词和段落过渡问题
5. **Step 1.5 内容实质性** - 检测学术锚点密度和幻觉风险

**Step 1.0 词汇锁定的核心价值**：
- 确保专业术语在整个De-AIGC处理过程中保持不变
- 用户可以主动选择哪些词汇需要保护
- 锁定规则自动注入到所有后续LLM调用的Prompt中
- 支持跨层传递（Layer 5 → 4 → 3 → 2 → 1）

每个子步骤都遵循：**检测 → 展示问题 → 用户点击触发AI分析 → 提供改进建议 → 用户决策 → 传递给下一步** 的交互模式。

这种设计确保了：
- **术语安全性**（锁定的专业词汇不会被意外修改）
- 检测的全面性（整合所有已有和待集成的检测器）
- 处理的有序性（从宏观到微观）
- 用户体验的友好性（清晰的问题展示和AI辅助建议）
- 技术实现的可行性（基于现有代码架构）
