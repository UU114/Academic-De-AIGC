针对于本项目的substep，需要做以下测试。
 # 1  c:\Users\TPY\Desktop\DEAI\test_documents\test_high_risk.txt 是本次测试的测试文本
 # 2 对于每一个单独的substep，进行独立测试，每个测试生成一个记录文件和一个报告
 # 3 测试内容包括：
  ## 3.1 建立单独的substep记录文档，记录输入文本，记录AI分析的提示词
  ## 3.2 记录调用了什么分析（功能+代码位置），如果有链式调用需要记录多次调用以及传递的值
  ## 3.3 分析结果（规则分析（如有）+llm分析），记录分析结果的问题
  ## 3.4 进行问题的多选，指定AI合并分析的功能
  ## 3.5 记录发送给AI的提示词,等待AI返回，针对AI输出，选择采纳
  ## 3.6 记录AI输出的文本 
 # 4 测试注意事项
  ## 4.1 对项目功能的测试操作，全程使用playwright. 需要进行链式的测试，即按照正常的使用顺序来进行每个substep的测试
  ## 4.2 每个功能的测试，需要测试前端显示内容与后端调用功能是否对应
  ## 4.3 测试完成后，需要对比记录文档内的输入文本、输出文本以及AI分析结果进行分析
   ### 4.3.1 查看提出的问题，对比前后的文本看是否真正解决了提出的问题
   ### 4.3.2 评估AI修改是否有效的降低了AIGC的特征，增加了人类写作的特征
   ### 4.3.3 评估4.3.2的效果,是否可以达到人类写作的水平
   ### 4.4.4 如果效果不好，再去查看提示词，分析原因。
  ## 4.4 所有的评分（CV等），在分析时做一次，在AI进行改造后，再做一次，需要对比
  ## 4.5 验证所有的数据（如段落数 单词数等）是否准确、有效
  ## 4.6 查看每个substep的输入是否为上一个substep的输出结果
  ## 4.7 全部测试完成以后，综合所有的报告
  ## 4.8 根据问题和结果，分析每个substep的功能，是否有重叠、缺少、偏差等需要优化的地方
  ## 4.9 本次测试单独建立文件夹
  ## 4.10 一定注意前端是否与后端一致
 # 5 其他
  ## 5.1 根据4.7总结的报告，和substep的设计，分析是否与预设的功能相符，是否一次处理即可达到预设的目标，哪些步骤需要修改，给出意见